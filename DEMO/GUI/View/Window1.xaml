<UserControl x:Class="GUI.View.Window1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.View"
             xmlns:vm="clr-namespace:GUI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="805"
             Style="{StaticResource Page_Style}">
    <UserControl.DataContext>
        <vm:Window1VM/>
    </UserControl.DataContext>
    
    <Grid Background="AliceBlue">
        <DatePicker HorizontalAlignment="Left" Margin="298,241,0,0" VerticalAlignment="Top" Height="29" Width="183"/>
        <Expander HorizontalAlignment="Left" Height="100" Header="Expander" Margin="356,158,0,0" VerticalAlignment="Top" Width="100">
            <Grid Background="#FFE5E5E5"/>
        </Expander>

    </Grid>
</UserControl>
